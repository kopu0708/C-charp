시작하기 앞서 책을 다시 펼친 날로부터 지금 7/13 이 날을 기점으로 한달 조금 안돼는 시간이 지났다. 

나름 성실히 매일 했지만 솔직히 공부한 시간만큼 진도를 못나간 날도 많고 얼마전에는 4일을 쉬었으니 그다지 성실하지 못한 태도로 임한 것 같지만, 막상 생각보다 많은 걸 익혔나 싶기도하다.

이 이야기를 왜 지금 하냐면 지금부터는 고급 문법에 속하는 내용들을 배우기 때문이다. 7장 부터 12장의 내용은 객체지향 프로그래밍을 이해하기 위한 내용들이었다.

이제 앞으로 계속 미뤄뒀던 고급 문법에 대한 공부를 시작하게 될 것이다. 스스로 의지를 다지기 위한 짧은 두서 없는 말이였다. 

# 예외처리 
예외처리.. 우리가 만약 정수형 숫자 만을 입력 받는 프로그램을 짯다고 가정해보자 

그런데 사람 마음이란게 참 알기 힘들지 않는가? 내 마음도 잘 모르겠는게 세상이다. 정수형만 넣으라고 분명 설명 해줬는데 분명 열에 하나는 소수나 문자열을 입력할 것이다. 

이 외에도 갑자기 컴퓨터가 꺼진다거나, 용량 이상의 파일이 한번에 날아와 감당이 안된다거나 이런 사고들이 있다. 

이러한 내가 가정한 상황을 벗어나는 사건들을 예외라 한다. 그리고 이러한 예외가 프로그램의 오류나 다운으로 이어지지 않게 처리하는걸 말 그대로 예외처리라고 하겠다.

그래서 어떻게 하는데요?

### try~catch문 
시도와 잡다? 이게 뭘까 예외를 처리하는 아주 기본이 되는 문법이다. 먼저 어떻게 생겼는지 부터 보자 
```C#
try
{
    // 실행할려는 코드
}
catch(예외_객체_1)
{
    // 예외가 발생했을 때의 처리
}
catch(예외_객체_2)
{
    // 예외가 발생했을 때의 처리
}
```
try 절의 코드 블록에는 예외가 일어나지 않을 경우에 실행되어야 할 코드들이 들어가고, catch 절에는 예외가 발생했을 때의 처리 코드가 들어간다.

말그대로 시도해보고? 예외가 던져지면 잡는다. 이렇게 이해하면 될 듯하다. 그리고 보다시피 하나의 시도에 여러개의 catch문을 놓을 수도 있다.

내 생각인데 if문이나 case랑 비슷한 것 같기도 하다. 분명한 차이점은 if,case 같은 분기문은 조건을 직접 검사하지만, try-catch문은 예외가 터지면 자동적으로 검사한다는 것이다.

참고로 catch는 위에서부터 차례로 검사해서 처음 일치하는 하나만 실행된다. 그래서 더 구체적인 예외를 위에, 포괄적인 예외(Exception)를 아래에 둬야 한다.

바로 예제를 만들어보자 [예제01](01_TryCatch.cs) 

근데 예제를 보면 IndexOutOfRangeException 이란게 나온다. 이게 뭘까? 

### System.Exception 클래스 
System.Exception 클래스는 모든 예외의 조상이다. c#의 모든 예외 클래스는 반드시 이 클래스로부터 상속받아야 하며 방금 봤던 Index어쩌고도 여기서부터 파생됐다. 

우리가 상속관계에서 배웠듯이 모든 예외 클래스는 System.Exception 형식으로 간주할 수 있고, 이 형식을 받는 catch문 하나면 모든 예외를 처리 할 수 있다는 말이다.

그럼 catch문 하나면 다 해결이네요. 그럴 리가 없다. 

System.Exception 형식은 프로그래머가 발생할 것으로 계산한 예외 말고도 다른 예외까지 받아 낼 수 있다. 만약 그 예외가 현재 코드가 아닌 상위 코드에서 처리해야 할 예외라면,

이 코드는 예외를 처리하는 대신 버그를 만들게 된다. 
```c#
try
{
    // 파일을 읽는 코드
}
catch (Exception e)  // 모든 예외를 다 잡아버림
{
    Console.WriteLine("에러 발생");
    // 사실은 심각한 시스템 오류였는데
    // "에러 발생"만 찍고 넘어가서 진짜 문제를 놓침
}
```
무분별한 catch (Exception)은 진짜 심각한 예외까지 삼켜버려서, 문제를 조용히 숨기는 결과를 낳는다. 그래서 예상되는 구체적 예외를 명시적으로 잡는 게 좋다.

### 예외 던지기 
catch문으로 예외를 잡는다는 것은 어디선가 예외를 던진다는 이야기이다. 그럼 어떻게 예외를 던질까?  throw 문을 이용해서 던진다. 

아까는 그런거 없었잖아요? 

앞선 예제에서는 C#의 CLR(런타임)이 자동으로 던진 것이다. 잘못된 인덱스에 접근하니 시스템이 알아서 IndexOutOfRangeException를 던져줬다.

throw는 내가 직접 예외를 던질 때 사용한다. 
```c#
// 시스템이 자동으로 던지는 경우 (throw 안 보임)
int x = arr[5];  // 범위 초과 → CLR이 자동으로 예외 던짐

// 내가 직접 던지는 경우 (throw 사용)
if (age < 0)
{
    throw new ArgumentException("나이는 음수가 될 수 없습니다.");
}
```
위와 같이 문법적으로 문제가 되지 않지만 내가 프로그램 규칙상 막아야하는 경우가 있다. 예를 들어 게임에서 HP는 음수가 되면 안된다. 하지만 코드상으로는 음수 대입이 가능하다.

이런경우 throw로 예외를 던져주어 처리하게 만드는 것이다. 

```c#
public int Hp
{
    set
    {
        if (value < 0)
            throw new ArgumentException("HP는 음수가 될 수 없습니다.");
        _hp = value;
    }
}
```
만약 저렇게 던져진 예외는 메소드를 호출하는 try~catch 문에서 받아낸다.  예제를 보자 [예제02](02_Throw.cs) 

예제에서 ArgumentException을 쓰지 않고 그냥 Exception으로 던졌는데 이는 앞서 말한 모든 예외가 Exception의 자식이라 Exception타입으로 다 잡힌다는 것을 보여주기 위함이다.  

throw는 보통 문으로 사용하지만 c# 7.0부터는 식으로도 사용할 수 있도록 개선되었다.

```C#
int? a = null; //nullable로 선언하고 null 넣은거
int b = a ?? throw new ArgumentNullException(); // a는 null이므로, b에 a를 할당하지 않고 throw 식이 실행된다.
// ?? : 왼쪽(a)이 null이 아니면 그 값을, null이면 오른쪽을 실행
```
참고로 ??연산자는 왼쪽 값이 null 이면 오른쪽 값을 반환한다. 

조건 연산자 안에서도 사용할 수 있다.
```C#
int[] array = new[] {1,2,3};
int index = 4;
int value = array[index >= 0 && index < 3 ? index : throw new IndexOutOfRangeException()];
// index가 0~2 범위면 array[index], 아니면 throw
// index가 4라서 범위를 벗어나므로 예외 발생
```
throw 문 — if 블록 안에서 독립적으로 사용

throw 식 — ?? 나 조건 연산자(?:) 안에서 값 대신 사용 (C# 7.0+)

### try~catch와 finally
try 블록에서 코드를 실행하다가 예외가 던져지면 프로그램의 실행이 catch로 뛰어넘어간다. 중간에 있는 코드들은 실행되지 않는다.

만약 실행되지 않은 코드들이 중요한 코드라면 버그가 생기는 원인이 된다. 예를 들어 파일을 열었으면 반드시 닫아야 하고, DB 연결을 열었으면 반드시 끊어야한다.

이러한 뒷 정리가 필요한 경우 finally가 사용된다. 왜냐하면 finally는 예외 발생 여부와 상관없이 항상 실행되기 때문이다. 

finally는 예외가 발생해서 catch로 갔을 때도, 예외가 아예 안 났을 때도, 심지어 try 안에서 return으로 빠져나가도 실행된다. 말 그대로 무조건이다.

물론 catch문에 써도 되지만 그렇게 되면 모든 catch문에 써야하기 때문에 코드가 반복해서 들어가게 된다. 
```C#
try
{
    // 실행할 코드
}
catch (Exception e)
{
    // 예외 처리
}
finally
{
    // 예외가 나든 안 나든 무조건 실행되는 코드
}
```
이런 식으로 try~catch 문의 마지막에 연결해서 사용한다. 그럼 이제 예제를 만들어 보자 [예제03](03_Finally.cs)


