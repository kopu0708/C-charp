# 챕터 7은 클래스에 대한 파일이다. 

클래스를 알기전에 객체지향 프로그래밍에 대해 알아야 한다. OOP라고도 하는데 코드 내 모든 것을 객체로 표현 할려는 프로그래밍 패러다임이다.

코드를 작성 할 때 모든 것을 다 표현할 필요는 없다. 객체의 주요 특징만 뽑아내면 된다.

그럼 객체의 가장 주요한 특징은 무엇일까?
바로 속성과 기능이다. 예를 들어 사람은 속성으로 피부색, 키, 몸무게 등이 있다. 기능으로 걷고 뛰고 먹고 자는 행위가 그 예시가 되겠다.

그럼 코드로 이 것들을 어떻게 표현 할 수 있을까? 속성은 데이터로, 기능은 메소드로 표현하면된다. 즉 객체는 데이터와 메소드로 이루어진다고 할 수 있다.

그래서 클래스는 뭔가요?

클래스는 객체를 만들기 위한 청사진이다. 클래스가 자동차 설계도라면 객체는 생산된 실제 자동차라고 비유 할 수 있다. 설게도에는 변경 가능한 속성(요소)들과 변경 불가능한 속성이 있다. 또한 설계도(클래스)는 실체가 없지만 이를 보고 만든 자동차는 실체를 가지고 기능(메소드)를 실행한다. 

우리가 자료형 변수를 만들때 쓰는 string도 이미 정의된 클래스이다. 그 뒤에 붙는 변수명은 객체이다. 참고로 객체를 인스턴스라고 부르기도 한다. 
아무튼 이런 식으로 객체에서 뽑아낸 속성과 기능을 클래스 안에 변수와 메소드로 표현 된다. 이런 식의 프로그래밍을 객체지향 프로그래밍이라고 한다.

클래스는 복합 데이터 형식이다. 클래스는 객체를 위한 청사진인 동시에 여러 메소드를 묶는 집합이다. 또한 코드에서 보는 클래스는 또 하나의 데이터 형식이다.
기본 데이터 형식을 조합해서 만든다는 말이다. 그리고 복합 데이터 형식은 참조 형식이다. 

* 클래스를 생성할 때에는 class 키워드와 클래스_이름을 이용해서 선언한다. 
* 클래스에서 선언되는 변수를 필드라고 한다. 그리고 필드와 메소드 프로퍼티 이벤트 클래스 등 클래스 내에 선언된 모든 요소를 멤버라고 한다.
* new 연산자와 생성자를 이용해 힙(Heap)에 객체를 생성하고, 스택(Stack)에 있는 참조 변수가 힙에 생성된 객체의 주소를 가리킨다.
 
클래스의 선언과 객체의 생성은 EXclass.cs 파일을 참고하자. [예제1](01_EXclass.cs)

### 생성자와 종료자 
생성자는 클래스를 선언할 때 명시적으로 생성자를 구현하지 않아도 컴파일러가 알아서 만들어준다. 이걸 기본 생성자라고 한다.

그럼 왜 직접 구현해야 하는 가?

객체를 다루다 보면 객체를 생성하는 시점에 객체의 필드를 원하는 값으로 초기화하고 싶을 때가 있다. 
즉 매개변수를 입력받아 원하는 값으로 필드를 초기화할 수 있는 최적의 장소가 바로 생성자이다. 

생성자는 클래스와 이름이 같고 반환 형식이 없다. 해당 클래스의 객체를 생성하는 역할 뿐이기 때문이다. 
참고로 생성자를 이용해 객체를 생성할 때에는 매개변수의 수와 형식이 일치해야한다. 

생성자 예시로 넘어가서 보자 Cat.cs 파일을 참고하면 된다.  [예제2](02_Cat.cs)

다음으로 종료자에 대해 이야기 하면 종료자의 이름은 클래스 이름 앞에 ~을 붙이면 된다.

생성자와 매개변수도 없고 한정자도 없다. 또한 여러 버전의 종료자를 만드는 오버로딩도 불가능하며 직접 호출할 수도 없다. 

그럼 어떻게 쓰냐?

CLR의 가비지 컬렉터가 객체가 소멸되는 시점을 판단해서 종료자를 호출 해준다. 그래서 유니티나 일반적인 C# 환경에서는 프로그래머가 직접 종료자를 구현할 일이 거의 없다. 오히려 잘못 쓰면 가비지 컬렉터의 동작을 방해해서 게임에 렉(프레임 드랍)을 유발하기도 한다. 따라서 종료자 예제는 넘어간다.

### 정적 필드와 메소드
static 메인 클래스에도 붙는 키워드라 익숙하다. 사전적 의미로는 정적이라는 뜻이다. 메소드나 필드가 클래스의 인스턴스가 아닌 클래스 자체에 소속되도록 지정하는 한정자이다.

저게 무슨 말이냐 한 프로그램 안에서 인스턴스는 여러 개가 존재 가능하다. 하지만 클래스는 단 하나만 존재한다. 똑같은 클래스는 두 개 이상 존재할 수 없다. 

어떤 필드가 클래스에 소속된다는 것은 곧 그 필드가 프로그램 전체에서 유일하게 존재한다는 것을 의미한다. 

그럼 뭐가 다르고 뭐가 좋냐? 

static이 붙은 필드는 프로그램 전체가 공유하고 별도의 인스턴스 생성 없이 클래스.변수명 으로 즉시 접근이 가능하다. 정적 메소드도 마찬가지고 

비정적 메소드는 인스턴스 메소드라고 한다. 보통 객체 내부 데이터르 이용해야 하는 경우는 인스턴스 메소드 내부 데이터를 이용할 일이 없는 경우에는 메소드를 정적으로 선언한다. 

그리고 static 키워드가 붙은 멤버나 클래스는 프로그램이 실행될 때 프로그램이 끝날 때까지 유지되는 별도의 특수한 메모리 영역(Static 영역)에 자리를 잡는다. (그래서 전역에서 접근이 가능함) 그리고 참고로 static을 붙여 클래스를 선언 하면 그 아래 모든 맴버들에게도 static 키워드를 붙여줘야 한다. 

global.cs 예제를 보자  [예제3](03_Global.cs)

### 객체 복사하기: 얕은 복사와 깊은 복사 
앞서 말 했듯이 클래스는 복합 데이터 형식이라 태생이 참조 형식이다. 참조 형식은 힙 영역에 객체를 할당하고 스택에 있는 참조가 힙 영역에 할당된 메모리를 가르킨다.

그래서 객체를 만들고 다른 객체를 하나더 만들어 단순히 source = copy 이런 식으로 쓴다던가 하면 힙에 있는 실제 객체가 아닌 스택에 있는 참조를 복사해서 받게된다. 

그러면 기껏 복사본을 만들었는데 원본도 같이 값이 수정되는 참사가 일어난다.

따라서 무언가 객체를 복사해두고 쓰고 싶다면 new 연사자를 써서 객체를 만들어 힙에 할당하고 반드시 꼭 필드 값을 하나 하나 넘겨주는 방식을 써야한다. 이를 깊은 복사라고 한다.(위와 같은 방식은 얕은 복사)

DeepAndShallow.cs를 참고하자 [예제4](04_DeepAndShallow.cs)

### this 키워드

바로 직전 예제를 보면 DeepCopy메소드에서 this 키워드를 사용했었다. 이게 뭐냐하면 객체가 자신을 지칭하는 키워드 이다. 

객체 외부에서는 객체의 필드나 메소드에 접근할 때 객체의 이름(변수 또는 식별자)를 사용한다면 객체 내부에서는 자신의 필드나 메소드에 접근할 때 this 키워드를 사용한다.

에를 들어 클래스에 Name이라는 필드가 있고 SetName 메소드가 있는데 매개변수가 Name 이라고 하면 문제가 생긴다. 이때 this키워드를 사용하면 문제를 해결 할 수 있다. 

this.Name은 클래스의 필드를 가르키고 Name은 매개변수를 말한다. 
~~~
class Employee
{
  private string Name;
  public void SetName(string Name)
  {
    this.Name = Name;
  }
}
~~~

이런 형태가 되겠다. 이러면 변수명을 고민할 필요가 없어지는 장점이 있다. 

더 자세한 에제는 This.cs를 보자 [예제5](05_This.cs)

### this() 생성자도 있다. 
이건 뭐가 다른건데요? ()가 붙었다. 물론 그게 다는 아니다. this()키워드는 자기 자신의 생성자를 가르킨다.

오직 생성자에서만 사용할 수 있고 그것도 생성자의 코드 블록 내부가 아닌 앞쪽에서만 말이다. 

객체를 만들 때 선택지를 여러개 주기 위해서 생성자를 여러개 만들다 보면 비슷한 초기화 코드가 중복되는 문제가 생긴다. this()는 이를 없애고 내가 해야 할 초기화 작업을 다른 생성자에게 넘긴다.

예시로 이해해보자 createPlayer.cs를 참고하자 [예제6](06_createPlayer.cs)

### 접근 한정자로 공개 수준 결정하기
public 이나 private 같은 키워드들이 있다. 이게 접근 한정자인데 객체지향 프로그래밍에는 은닉성이라는 것을 요구한다.

그게 뭐냐? 객체지향 프로그래밍의 3대 특성 중 하나로 은닉성, 상속성, 다형성이 있다. 그중 하나이다. 클래스에 선언 되어있는 필드와 메소드 중에 사용자에게 노출 하고 싶은 것이 있는 반면 그렇지 않는 것도 있다. 

특히 필드는 상수를 제외하고는 무조건 감추는 것이 좋다.  아무튼 접근 한정자는 감추고 싶은 것은 감추고 보여주고 싶은 것은 보여줄 수 있게 코드를 수식하며 메소드 프로퍼티 등 모든 요소에 사용할 수 있다 ㅇ

총 6가지가 있으며 다음과 같다.

* public : 클래스의 내부/외부 모든 곳에서 접근할 수 있다.
* protected : 클래스의 외부에서는 접근할 수 없지만, 파생 클래스에서는 가능하다.
* private : 클래스 내부에서만 접근 가능하다. 파생 클래에서는 접근 불가능
* internal : 같은 어셈블리에 있는 코드에서만 public으로 접근 할 수 있다. 다른 어셈블리에 있는 코드에서는 private과 같은 수준의 접근성
* protected internal : 같은 어셈블리에 있는 코드에서만 protected로 접근할 수 있다. 다른 어셈블리에 있는 코드에서는 private과 같은 수준의 접근성
* private protected : 같은 어셈블리에 있는 클래스에서 상속받은 클래스 내부에서만 접근할 수 있다.

참고로 접근 한정자를 수식하지 않으면 기본 값은 private이다. 그리고 같은 어셈블리에 있는 코드라는 말은 같은 프로젝트 안에서 하나의 .exe나 .dll로 묶여서 포장된다는 말이다. 즉 외부의 프로그램이 못 건드리게 한다는 거임

예제를 보자 ProtectedLv.cs 를 참고하면 된다. [예제7](07_ProtectedLv.cs)

### 상속으로 코드 재활용하기 
바로 위에 있는 예제 7번을 봐도 나오는데 class 이름 옆에 콜론(:) 이 붙어 있고 그 옆에 다른 클래스 이름이 있는 형태가 있다. 이걸 파생 클래스라고 한다. 

기반 클래스의 모든 것을 물려받게 갖게 된다. 단 private로 선언된 멤버는 에외이다. 

또한 파생 클래스는 상속받은 멤버들 이외에 자신만의 고유한 멤버를 가질 수 있다. 파생 클래스의 객체를 생성할 떄 내부적으로 기반 클래스의 생성자를 호출한 후에 자신의 생성자를 호출하고

객체가 소멸될 때는 반대의 순서로(파생 클래스 -> 기반 클래스) 종료자를 호출한다. 

그럼 기반 클래스의 생성자가 매개변수를 입력받도록 되어 있다면 파생 클래스의 인스턴스를 생성할 때 호출되는 기반 클래스의 생성자에 어떻게 매개변수를 넘겨줄까?

base라는 키워드가 있다. this같은건데 this가 자기 자신을 가르킨다면 base는 기반 클래스를 가리킨다. base 키워드를 통해 기반 클래스의 멤버에 접근할 수 있다. 

base()는 기반 클래스의 생성자이다. base()에 매개변수를 넘겨 호출하면 생성자를 통해 필드를 초기화 할 수 있다.

에제를 보자 Base.cs를 참고하면 된다. [예제8](08_Base.cs)

참고로 sealed class 클래스명 이렇게 앞에 sealed 키워드를 붙여주면 상속이 불가능한 클래스를 선언할 수 있다. 이를 봉인 클래스라고 한다. 

### 기반 클래스와 파생 클래스 사이의 형식 변환 
종이 다른 서로다른 동물이 하나의 분류로 묶이는 것은 흔하다. 예를 들어 표유류나 영장류처럼 이 개념은 클래스와 비슷하다. 표유류 클래스를 상속받은 우리가 있다.

아니면 Human 클래스가 있다고 치자 거기에는 피부색, 나이, 성격 같은 멤버들이 있다. 그리고 이 클래스를 상속 받아 만들어진 인류가 70억 명이 넘는다.

어느날 미친 마법사가 대량학살 마법을 코딩으로 만들었다. kill()이라는 메소드로 그런데 70억번 kill()메소드를 오버로딩 할 수 없지 않는가? 

하지만 어찌됐든 70억명 모두 인간이다. 따라서 딱 하나의 kill()메소드로 70억명의 인간 클래스에 사용할 수 있다. 

이처럼 파생 클래스의 인스턴스(70억 명)를 기반 클래스(Human) 형식으로 변환하여 부르는 것을 **업캐스팅(Upcasting)*이라고 부르며, 이를 통해 코드의 재사용성과 생산성을 극대화할 수 있다.

이처럼 기반 클래스와 파생 클래스 사이에서 족보를 오르내리는 형식 변환이 가능하며, 파생 클래스의 인스턴스는 기반 클래스의 인스턴스로도 사용할 수 있다. 

그리고 이것을 도와주는 연산자 두개가 있는데 is와 as이다. 

* is는 객체가 해당 형식에 해당하는지 검사하여 결과를 bool 값으로 반환한다.
* as는 형식 변환 연산자와 같은 역할이지만 형식 변환 연산자가 변환에 실패하는 경우 예외를 던지는 반면에 as 연산자는 객체 참조를 null로 만들어준다.

as연산자는 값 형식의 객체에서는 사용하지 못한다. 말로만 하면 이해가 힘드니 예제로 넘어가자 TypeCasting.cs를 보자  

[예제9](09_TypeCasting.cs) 

### 오버라이딩과 다형성 
객체지향 프로그램잉에서 다형성은 객체가 여러 형태를 가질 수 있음을 의미한다. 자신으로부터 상속받아 만들어진 파생 클래스르 통해 다형성을 실현한다.

앵그리버드를 생각해보자 기본 기능은 새총으로 날아가는 bird라는 클래스가 있을것이다.(아마도) 그리고 그안에는 shoot()이라는 메소드가 있다고 치자

알다시피 앵그리버드에는 부매랑 친구도 있고 폭발하는 친구도 있다. 이를 구현하기 위해 bird 클래스를 물려받을 것이고 각자 고유한 메소드를 가진 클래스를 구현할 것이다.

하지만 그냥 상속 받아서는 이들을 구현 할 수 없다. shoot() 메소드를 재정의 해야하기 떄문이다. 즉 오버라이딩이 필요하다.

메소드를 오버라이딩하기 위해서는 조건이 필요하다. 일단 먼저 오버라이딩할 메소드가 virtual 키워드로 한정되어 있어야 한다. 그리고 자식 클래스의 오버라이딩하는 측의 메소드는 override로 한정해줘야한다. 

물론 메소드를 다 뜯어 고칠 필요는 없다. 그럴떄는 그냥 base.메소드명() 이렇게 써주면 된다. 마지막으로 private로 수식된 메소드는 오버라이딩할 수 없다.
(어차피 파생 클래스에서 보이지도 않음)

overrideMethod.cs 예제를 보자! [예제10](10_overrideMethod.cs) 

### 메소드 숨기기 
메소드를 숨길수도 있다. 기껏 만들고 왜 숨기나요? 그러게 말이다. 

물론 이유가 있다. 일단 메소드를 만들때 virtual로 선언했는데 사실 사람일 이라는게 어떻게 될지 모르지 않는가 향후 이 메소드가 오버라이딩될지 안될지는 모르는 일이다.

그래서 메소드 숨기기가 뭐냐 하면 기반 클래스에서 구현된 버전의 메소드를 감추고 파생 클래스에서 구현된 버전만 보여주는 것을 말한다. 

파생 클래스 버전의 메소드를 new 한정자로 수식함으로써 할 수 있다.(생성자를 호출할 때 사용하는 new 연산자와의 완전히 다른 녀석임)

그럼 메소드 오버라이딩이 왜 있는데요? 오버라이딩이랑 다른거다. 말 그대로 숨기기로 메소드가 노출되어 실행 될 수 있다. 다형성을 표현하는데 한계가 있다.

부모 클래스로 변수를 만들고 자식 클래스로 객체를 만들면 숨겨진 메소드가 실행이 된다. 

이게 왜 필요하냐면 내가 쓴 코드이면 위로 올라가서 virtual을 달아주면 되지만 
