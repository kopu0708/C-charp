# 배열과 컬렉션 그리고 인덱서
챕터 10은 배열에 대한 챕터인 만큼 큐나 스택 같은 자료형도 나온다. 게임 개발과 알고리즘 문제 풀이를 조금 해본 경험상 상당히 중요한 내용일 것이다.


### 배열 
프로그램을 작성하다 보면 같은 성격을 띤 다수의 데이터를 한 번에 다뤄야 하는 경우가 자주 생긴다. 

단순히 내가 학원을 차렸다. 그래서 원생들 점수를 저장하고 가공하거나 해야한다고 상황을 가정해 보자, 근데 학원이 너무 커지는 바람에 원생이 100명이 넘어갔다.

그럼 우리는 점수를 저장하는 변수를 100개 만들어야 할까? 

당연히 그렇지 않다. 틀린 방법은 아니지만 확실히 더 좋은 방법이 있을거다. 그리고 그걸 이미 알고 있다. 배열이다.

```c#
데이터_형식[] 배열_이름 = new 데이터_형식[용량];
```
이런 형식으로 선언하다. 저 용량을 5라고 쓰면 0부터 4까지의 숫자가 붙은 5개의 상자가 생긴다고 이해하면 직관적이다. 

여자저차 아는 내용은 넘어가서 바로 예제로 넘어가자 [예제01](Array.cs)

참고로 배열의 가장 마지막에 접근하고 싶으면 배열 길이의 -1 이다. ^연산자를 써도 됨 ex) ^3은 뒤에서 3번째 
### 배열을 초기화 하는 방법 
초기화 하는 방법이다. 3가지 한번에 가겠다. 

```c#
string[] array1 = new string[3] {"안녕", "반가워", "요"}; //이렇게 크기 만큼 초기자 나열
string[] array2 = new string[] {"안녕", "반가워", "요"}; //아니면 크기를 정하지 않고 원하는 만큼 초기자를 나열 그러면 이 크기만큼의 배열이 생성됨
string[] array3 = {"안녕", "반가워", "요"}; // new 연산자와 형식과 용량등을 모두 생략한 버전 
```
이게 전부다.

### System.Array
c#은 모든 것이 객체이다. 배열도 객체이다. 당연히 기반이 되는 형식이 있다. 배열은 System.Array 클래스에 대응된다. 

저기 있는 메소드 프로퍼티들 쓸 수 있다는 말임 

sort(), BinarySearch<T>(), IndexOf(), TrueForAll<T>()...등등 

이렇게 있는데 의문이 하나 있다. <T>() 이런게 뒤에 붙은 친구들은 뭐란 말인가? 

<T>는 형식매개변수라고 하는데, 이들 메소드를 호출할 때는 T 대신 배열의 기반 자료형을 인수로 입력하면 컴파일러가 해당 형식에 맞춰 동작하도록 메소드를 컴파일한다.

자세한 내용은 일반화 프로그래밍에서 다루기로 하고 다음으로 넘어가자 [예제02](02_MoreOnArray.cs)

### 배열 분할하기 
